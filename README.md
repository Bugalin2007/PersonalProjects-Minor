This repository is used to store example and practice codes of my Java learning

Introduction of corresponding content are presented at the beginning of each program

The following list is generated by DeepSeek for giving inspiration on what to learn:

## 📅 第一阶段（3个月）：Java核心 & 开发基石

**核心目标**：夯实Java SE基础，掌握开发核心工具（Git），建立面向对象思维。

### Weekly Topics：

1.  **Java基础强化**：八种基本数据类型、`String`不可变性、运算符、Scanner输入。
    -   作业（化学）：编写一个摩尔浓度计算器。
    -   作业（通用）：编写一个命令行“石头剪刀布”游戏。
2.  **流程控制**：`if-else`, `switch`, `for`, `while`, `break`, `continue`。
    -   作业（化学）：判断一个年份是否为闰年（用于记录实验日期）。
    -   作业（通用）：打印九九乘法表和各种形状的星号（*）图案。
3.  **方法**：定义、重载、递归入门、传参机制（值传递）。
    -   作业（化学）：编写方法计算斐波那契数列（模拟细胞分裂？）。
    -   作业（通用）：编写递归方法计算一个数的阶乘。
4.  **面向对象基础（OOP）**：类与对象、构造方法、`this`关键字、封装（访问修饰符）。
    -   作业（通用）：创建一个`BankAccount`类，模拟存款、取款、查询余额。
5.  **数组**：一维数组、二维数组、遍历、`Arrays`工具类。
    -   作业（化学）：用二维数组存储并打印一份“元素周期表”的局部。
    -   作业（通用）：实现冒泡排序算法对整数数组进行排序。
6.  **Git与GitHub版本控制**：安装、本地仓库（`init`, `add`, `commit`）、远程仓库（`remote`, `push`, `pull`）、.gitignore。
    -   作业（通用）：在GitHub创建你的第一个仓库，并将本周的代码推送上去。
7.  **Java集合框架（List）**：`ArrayList`, `LinkedList`，泛型。
    -   作业（通用）：用`ArrayList`管理一个“任务待办清单”（命令行交互）。
8.  **Java集合框架（Map & Set）**：`HashMap`, `HashSet`，键值对思想。
    -   作业（化学）：用`HashMap`实现元素符号到元素名称的快速查询。
    -   作业（通用）：统计一篇文章中每个单词出现的次数（忽略大小写）。
9.  **异常处理**：`try-catch-finally`，自定义异常类。
    -   作业（通用）：完善`BankAccount`类，取款时处理“余额不足”的异常。
10.  **综合练习与代码风格**：编写清晰、可读的代码，编写简单的Javadoc注释。
    -   作业（通用）：选择之前的一个作业进行重构，改善代码结构并添加注释。

### 大作业（三选二）：

1.  **化学试剂管理系统**：命令行程序，使用集合类管理试剂信息（名称、数量、位置），支持增删改查和库存预警。
2.  **LCR（左中右）骰子游戏模拟器**：模拟一个简单的多人骰子游戏，练习OOP和流程控制。
3.  **个人密码管理器（原型）**：使用`HashMap<String, String>`模拟一个存储账号密码的程序（无需加密，重点在程序结构）。

---

## 📅 第二阶段（3个月）：数据结构、算法与IO

**核心目标**：掌握核心数据结构与算法，学会处理文件和数据持久化（JSON）。

### Weekly Topics：

1.  **算法复杂度分析**：大O表示法，时间与空间复杂度。
    -   作业（通用）：分析线性查找和二分查找的复杂度。
2.  **数据结构：链表**：实现一个单链表，支持添加、删除、查找。
    -   作业（通用）：用自己实现的链表，做一个“最近使用的元素”（LRU）缓存模拟。
3.  **数据结构：栈和队列**：实现栈（Stack）和队列（Queue）。
    -   作业（化学）：用栈检查一个化学方程式中的括号是否匹配（如 `((a)(b))` ）。
    -   作业（通用）：用队列模拟一个打印机任务队列。
4.  **算法：递归深入**：理解递归调用栈，解决经典问题。
    -   作业（通用）：用递归实现汉诺塔问题的求解（打印步骤）。
5.  **算法：深度优先搜索（DFS）**：应用在树和图上的遍历。
    -   作业（通用）：使用DFS遍历一个目录下的所有文件（打印文件树）。
6.  **算法：广度优先搜索（BFS）**：求最短路径（无权图）。
    -   作业（通用）：在一个二维网格中（0可走，1为障碍），使用BFS找到从起点到终点的最短步数。
7.  **Java IO基础**：`File`类，字节流`InputStream/OutputStream`，字符流`Reader/Writer`。
    -   作业（通用）：实现一个文件复制工具。
8.  **JSON处理（Jackson库）**：`ObjectMapper`，Java对象与JSON的序列化和反序列化。
    -   **Package**: `com.fasterxml.jackson.databind`
    -   作业（通用）：定义一个`User`类（id, name, email），将其序列化为JSON字符串并写入文件，再从文件读回并反序列化为对象。
9.  **Java日志框架（SLF4J + Logback）**：配置和使用，替代`System.out.println`。
    -   **Packages**: `org.slf4j`, `ch.qos.logback`
    -   作业（通用）：在之前的任意一个项目中集成日志，记录程序运行状态。
10.  **正则表达式**：`Pattern`和`Matcher`类，用于文本校验和提取。
    -   作业（通用）：编写正则表达式验证邮箱地址、手机号和身份证号的格式（简化规则）。

### 大作业（三选二）：

1.  **化合物数据库（文件版）**：使用JSON文件作为存储，实现化合物的增删改查（CRUD）功能，综合运用集合、IO和Jackson。
2.  **迷宫求解器**：从文件读取一个迷宫地图，使用DFS和BFS两种算法分别寻找出口路径，并可视化打印结果。
3.  **网络爬虫原型（本地）**：编写程序读取一个本地HTML文件，使用正则表达式提取其中的所有链接（`<a href="...">`）和图片（`<img src="...">`）。

---

## 📅 第三阶段（3个月）：GUI、并发与网络基础

**核心目标**：使用JavaFX构建现代桌面应用，理解多线程和网络通信。

### Weekly Topics：

1.  **JavaFX基础**：Stage, Scene, Node, Layouts (VBox, HBox, BorderPane), CSS样式。
    -   **Package**: `javafx.*`
    -   作业（通用）：创建一个登录窗口界面，包含用户名、密码输入框和按钮。
2.  **JavaFX事件处理**：`EventHandler`，`Button`点击事件，`TextField`输入事件。
    -   作业（通用）：为登录界面添加事件，点击按钮后在控制台打印输入的用户名和密码。
3.  **JavaFX控件（一）**：`TableView`, `ObservableList`，数据绑定。
    -   作业（通用）：用`TableView`做一个学生信息管理界面，支持显示、添加、删除。
4.  **JavaFX控件（二）**：`ListView`, `TreeView`, `MenuBar`。
    -   作业（通用）：使用`TreeView`做一个文件浏览器，显示指定目录的结构。
5.  **Java多线程（一）**：`Thread`, `Runnable`, 线程生命周期。
    -   作业（通用）：在JavaFX中创建一个进度条，用一个后台线程模拟耗时任务，更新进度条（注意线程安全！）。
6.  **Java多线程（二）**：线程同步：`synchronized`关键字，`Lock`。
    -   作业（通用）：模拟多个线程同时向一个银行账户存款， without 和 with 同步，观察结果差异。
7.  **网络编程（一）**：TCP Socket编程，`ServerSocket`, `Socket`。
    -   作业（通用）：编写一个简单的聊天程序的服务器和客户端（命令行），客户端发送消息，服务器原样返回。
8.  **网络编程（二）**：HTTP协议客户端，使用`java.net.HttpURLConnection`。
    -   作业（通用）：编写程序，请求一个公共API（如 `api.github.com`），并解析返回的JSON数据。
9.  **单元测试（JUnit 5）**：编写可测试的代码，`@Test`, `@BeforeEach`, 断言。
    -   **Package**: `org.junit.jupiter.api`
    -   作业（通用）：为你之前实现的某个算法（如排序、DFS）编写单元测试。
10.  **构建工具（Maven）深入**：多模块项目，依赖范围，常用插件。
    -   作业（通用）：用Maven创建一个包含核心模块和GUI模块的项目。

### 大作业（三选二）：

1.  **化合物数据库（桌面版）**：使用JavaFX开发图形界面，替代之前的命令行版本，支持通过表格展示数据、表单添加、JSON文件导入导出。
2.  **多线程文件下载器**：图形界面，输入文件URL和保存路径，能显示下载进度，并且支持暂停和继续（可选）。
3.  **实时天气查询桌面应用**：使用JavaFX构建界面，通过HTTP请求获取城市天气API数据（如和风天气），并解析JSON展示天气信息。

---

## 📅 第四阶段（3个月）：高级算法、数据库与系统交互

**核心目标**：掌握关键算法，连接数据库，实现程序与操作系统的交互。

### Weekly Topics：

1.  **算法：动态规划（DP）入门**：理解重叠子问题与最优子结构。
    -   作业（通用）：使用DP求解经典的斐波那契数列和第n个丑数。
2.  **算法：动态规划（DP）经典问题**：背包问题（01背包）。
    -   作业（通用）：给定一组物品的重量和价值，计算在最大重量限制下能装入的最大价值。
3.  **算法：贪心算法**：理解贪心选择性质。
    -   作业（通用）：使用贪心算法解决区间调度问题（最多能参加多少个不重叠的会议？）。
4.  **数据库与SQL基础**：SQLite安装和使用，DDL（CREATE TABLE）, DML（INSERT, SELECT, UPDATE, DELETE）。
    -   作业（通用）：在SQLite中创建学生表，并编写SQL语句进行增删改查练习。
5.  **Java数据库连接（JDBC）**：`Connection`, `Statement`, `PreparedStatement`, `ResultSet`。
    -   **Package**: `java.sql`
    -   作业（通用）：用JDBC连接SQLite，将学生表的查询结果打印到控制台。
6.  **ORM入门（Hibernate或MyBatis选一）**：配置，实现基本的对象映射。
    -   **Package**: `org.hibernate.*` OR `org.mybatis.*`
    -   作业（通用）：使用ORM框架完成对学生表的CRUD操作。
7.  **进程与系统调用**：`ProcessBuilder`类，执行系统命令。
    -   **Package**: `java.lang`
    -   作业（通用）：编写程序，调用系统命令（如`ls -l`(Linux)或`dir`(Windows)）并获取输出。
8.  **Java调用Python脚本**：使用`ProcessBuilder`调用Python解释器执行`.py`文件并传递参数。
    -   作业（化学）：用Java做GUI，调用一个用Python写的科学计算脚本（如用NumPy进行拟合）并显示结果。
9.  **设计模式（一）：单例模式**：确保一个类只有一个实例。
    -   作业（通用）：将你的数据库连接工具类改造为单例模式。
10.  **设计模式（二）：观察者模式**：对象间的一对多依赖关系。
    -   作业（通用）：实现一个简单的消息发布中心，允许观察者订阅和接收消息。

### 大作业（三选二）：

1.  **算法可视化工具**：选择一种算法（如排序、DFS、BFS、背包），用JavaFX动画演示其运行过程。
2.  **个人财务管理系统**：使用JavaFX + JDBC/SQLite，记录收入支出，支持分类统计和图表展示（可用JFreeChart）。
3.  **自动化测试脚本运行器**：图形界面，配置和管理你的Python或Shell测试脚本，一键运行并汇总结果。

---

## 📅 第五阶段（3个月）：Web基础、安全与高级特性

**核心目标**：从桌面走向网络，了解Web开发与安全基础，探索Java的强大高级特性。

### Weekly Topics：

1.  **HTTP协议详解**：请求方法（GET/POST/PUT/DELETE）、状态码、报文头、报文体。
    -   作业（通用）：使用`HttpURLConnection`手动构造一个带`Authorization`头的GET请求，访问GitHub API获取你的用户信息。
2.  **构建简易HTTP服务器**：基于TCP Socket，解析HTTP请求行和头，返回静态资源（如HTML文件）。
    -   作业（通用）：实现一个微型服务器，当访问`/hello`时返回`"Hello World"`文本，访问`/index.html`时返回一个HTML文件。
3.  **Servlet入门**：Servlet生命周期（`init`, `service`, `destroy`），`HttpServletRequest`, `HttpServletResponse`。
    -   **Package**: `javax.servlet.http.*`
    -   作业（通用）：编写一个Servlet，处理GET请求并返回当前服务器时间。
4.  **Servlet容器（Tomcat）**：安装、部署WAR包、理解Web容器的作用。
    -   作业（通用）：将你的Servlet部署到本地Tomcat并成功访问。
5.  **RESTful API设计与JAX-RS**：理解RESTful风格，使用Jersey框架创建API。
    -   **Packages**: `org.glassfish.jersey.*`
    -   作业（通用）：用Jersey实现一组对`Book`资源的RESTful API（`/books` [GET, POST], `/books/{id}` [GET, PUT, DELETE]）。
6.  **安全基础：哈希与加密**：摘要算法（MD5, SHA-256）、对称加密（AES）、非对称加密（RSA）概念。
    -   **Package**: `java.security`, `javax.crypto`
    -   作业（通用）：编写一个工具类，使用SHA-256对用户密码进行哈希，使用AES加密/解密一段文本。
7.  **常见Web安全漏洞**：SQL注入、XSS（跨站脚本）、CSRF（跨站请求伪造）的原理与防护。
    -   作业（通用）：修复一个存在SQL注入漏洞的JDBC查询代码（使用`PreparedStatement`）。
8.  **Java反射机制（Reflection）**：`Class`类，获取类信息、方法、字段，动态创建对象和调用方法。
    -   **Package**: `java.lang.reflect`
    -   作业（通用）：写一个方法，接受一个对象作为参数，遍历其所有字段并打印字段名和值。
9.  **Java注解（Annotation）**：元注解（`@Target`, `@Retention`），自定义注解，使用反射处理注解。
    -   作业（通用）：定义一个`@ExcelColumn`注解，标记在字段上，用于后续模拟导出Excel时定义列名。
10.  **函数式编程接口与Lambda深入**：`java.util.function`包下的`Predicate`, `Function`, `Consumer`, `Supplier`，方法引用。
    -   作业（通用）：使用`Stream API`和`Predicate`对一个对象集合进行复杂的过滤和映射操作。

### 大作业（三选二）：

1.  **简易HTTP服务器（增强版）**：基于Socket，支持静态文件服务（如HTML, CSS, JS, 图片），并实现简单的Servlet机制（通过URI映射到不同的处理类）。
2.  **RESTful 化合物查询API服务**：使用Jersey框架，提供对化合物数据的JSON API接口，支持通过ID查询、按条件过滤（如`/compounds?element=O`）。
3.  **文件加密/解密工具（GUI）**：使用JavaFX制作界面，用户选择文件、输入密钥，可选择使用AES进行加密或解密操作。

---

## 📅 第六阶段（3个月）：Spring Boot、系统设计与性能

**核心目标**：掌握现代Java企业级开发核心框架Spring Boot，学习系统设计的基本思想，关注应用性能。

### Weekly Topics：

1.  **Spring Boot基础**：核心概念（IoC控制反转, DI依赖注入）、自动配置、 starter 概念。
    -   **Packages**: `org.springframework.boot.*`
    -   作业（通用）：使用Spring Initializr创建项目，编写一个`@RestController`，返回`"Hello Spring Boot"`。
2.  **Spring Data JPA**：使用JPA注解映射实体类，`CrudRepository`接口，定义查询方法。
    -   **Packages**: `org.springframework.data.*`, `javax.persistence.*`
    -   作业（通用）：定义`Book`实体和`BookRepository`，实现基本的CRUD功能。
3.  **构建RESTful API（Spring版）**：`@RestController`, `@RequestMapping`, `@PathVariable`, `@RequestBody`。
    -   作业（通用）：用Spring Boot重构第五阶段的`Book` RESTful API。
4.  **系统设计基础：缓存**：缓存的作用、粒度、缓存策略（Cache-Aside）。
    -   作业（通用）：使用Caffeine或Guava Cache，在内存中缓存频繁查询的化合物数据。
5.  **系统设计基础：消息队列**：解耦、削峰填谷的概念，JMS或AMQP简介。
    -   作业（通用）：使用Docker安装RabbitMQ或ActiveMQ，编写简单的生产者和消费者程序。
6.  **数据库连接池**：HikariCP配置与使用，理解其相对于传统JDBC连接获取的优势。
    -   **Package**: `com.zaxxer.hikari.*`
    -   作业（通用）：在Spring Boot项目中配置HikariCP，并观察不同并发下的性能差异。
7.  **JVM性能监控基础**：使用`jconsole`或`jvisualvm`监控本地Java进程的CPU、内存、线程状态。
    -   作业（通用）：监控一个存在轻微内存泄漏的程序（例如，不断向静态集合添加对象），观察GC和内存变化。
8.  **Docker基础**：镜像（Image）、容器（Container）、Dockerfile编写。
    -   作业（通用）：为你之前的一个Spring Boot项目编写Dockerfile，并构建镜像。
9.  **设计模式（三）：工厂模式**：简单工厂、工厂方法模式。
    -   作业（通用）：使用工厂模式创建一个图表（Chart）工厂，根据传入类型返回折线图、柱状图等不同实例。
10.  **设计模式（四）：代理模式**：静态代理、动态代理（JDK `Proxy`）的概念。
    -   作业（通用）：使用JDK动态代理为一个方法调用添加日志记录功能（在方法执行前后打印日志）。

### 大作业（三选二）：

1.  **全栈化合物管理系统（Spring Boot版）**：使用Spring Boot + Spring Data JPA + H2/SQLite 构建后端RESTful API，并提供**简易**的HTML前端（使用Thymeleaf模板或纯静态页面调用API）进行展示和管理。
2.  **高性能缓存模拟库**：实现一个内存缓存库，支持设置过期时间、最大容量，并提供多种淘汰算法（LRU、LFU）供选择。
3.  **Docker化Spring Boot应用**：将你的化合物API服务打包成Docker镜像，上传到你的Linux服务器并运行起来，确保能从外部访问。

---

## 📅 第七阶段（3个月）：分布式概念与前沿技术

**核心目标**：理解分布式系统的基本概念，接触微服务架构和云原生技术，探索Java前沿发展。

### Weekly Topics：

1.  **分布式系统概念**：CAP定理、BASE理论、一致性哈希算法及其应用。
    -   作业（通用）：实现一个简单的一致性哈希算法，模拟节点（服务器）的添加和移除，以及数据的分布。
2.  **微服务概念与Spring Cloud入门**：服务注册与发现（Eureka）、客户端负载均衡（Ribbon）。
    -   **Packages**: `org.springframework.cloud.*` (e.g., `spring-cloud-starter-netflix-eureka-client`)
    -   作业（通用）：创建两个Spring Boot应用：一个服务提供者（如化合物服务），一个服务消费者，并将它们注册到Eureka Server。
3.  **服务间通信**：OpenFeign声明式REST客户端。
    -   **Packages**: `org.springframework.cloud.openfeign.*`
    -   作业（通用）：在服务消费者中使用Feign调用服务提供者的API。
4.  **API网关（Gateway）**：统一入口、路由、过滤、限流。
    -   作业（通用）：使用Spring Cloud Gateway为你的微服务demo配置路由规则。
5.  **GraphQL入门**：对比RESTful，定义Schema，查询（Query）和变更（Mutation）。
    -   **Packages**: `com.graphql-java.*`
    -   作业（通用）：创建一个GraphQL服务，提供对化合物数据的复杂查询（例如，一次查询获取化合物及其所有元素详情）。
6.  **WebSocket实时通信**：`@ServerEndpoint`, 全双工通信。
    -   **Packages**: `javax.websocket.*`
    -   作业（通用）：实现一个简单的WebSocket Echo服务，客户端发送消息，服务器立即返回。
7.  **GraalVM与原生镜像（Native Image）**：AOT编译、优势与局限。
    -   作业（通用）：尝试使用Spring Native或GraalVM Native Build Tools将一个简单的CLI工具编译为原生可执行文件。
8.  **Java新特性探秘（选讲）**：`var`、`record`、`sealed class`、`pattern matching`等现代Java语法。
    -   作业（通用）：使用`record`简化你的数据传输对象（DTO），使用`pattern matching`简化`instanceof`判断。
9.  **持续集成/持续部署（CI/CD）概念**：GitHub Actions workflow基本配置。
    -   作业（通用）：为你的一个GitHub仓库配置CI，实现代码推送后自动运行单元测试。
10.  **技术选型与架构设计思考**：如何为不同的项目需求选择合适的技术栈。
    -   作业（通用）：为你计划中的大作业画一个简单的架构图，并说明技术选型理由。

### 大作业（三选二）：

1.  **微服务 demo（Spring Cloud）**：将化合物查询、用户认证、数据统计等功能拆分为多个独立的Spring Boot微服务，并集成Eureka和Gateway。
2.  **实时协作白板**：使用WebSocket实现一个多用户实时画板，所有用户的绘制操作实时同步。
3.  **GraalVM原生编译实践**：将一个简单的Spring Boot CLI应用（或你的化合物API）编译为原生可执行文件，对比启动速度和内存占用。

---

## 📅 第八阶段（3个月）：综合项目、复盘与展望

**核心目标**：融会贯通，完成一个综合性项目，复盘整个学习旅程，规划未来技术方向。

### Weekly Topics：

1.  **项目规划与需求分析**：使用UMl用例图、功能列表等方式明确项目范围和功能。
    -   作业（通用）：为你选择的大作业编写详细的需求说明文档。
2.  **系统架构设计**：绘制系统架构图（组件、关系、技术选型）。
    -   作业（通用）：为你的大作业绘制架构图，并撰写设计说明。
3.  **数据库设计**：概念模型、逻辑模型（ER图）、物理建表。
    -   作业（通用）：为你的大作业设计数据库表结构。
4.  **模块拆分与开发**：前后端分离、接口约定（OpenAPI/Swagger）。
    -   作业（通用）：使用Swagger为你的后端API生成交互式文档。
5.  **测试策略**：单元测试、集成测试、压力测试（JMeter入门）。
    -   作业（通用）：使用JMeter对你API的一个关键接口进行压力测试，生成报告。
6.  **部署与运维**：在你的云服务器上部署最终项目（可能包含数据库、多个微服务、网关等）。
    -   作业（通用）：编写部署脚本或使用Docker Compose整理所有依赖服务。
7.  **代码重构与优化**：回顾代码，应用设计模式，改善可读性和可维护性。
    -   作业（通用）：对你项目中你认为最混乱的一个模块进行重构。
8.  **技术文档编写**：编写项目的README、安装手册、用户手册。
    -   作业（通用）：为你完成的项目撰写一份清晰的技术文档。
9.  **学习路径复盘**：回顾两年所学，整理知识图谱，识别强项和薄弱环节。
    -   作业（通用）：绘制你自己的Java/计算机知识脑图。
10.  **未来规划**：思考下一步学习方向（如深入分布式、探索AI/ML、钻研特定领域框架等）。
    -   作业（通用）：写一篇博客，总结你的学习历程并对未来提出展望。

### 大作业（三选二）：

1.  **“虚拟化学实验模拟与学习平台”**：一个综合性项目。可包含：化合物数据库管理、实验流程模拟（图形化展示）、实验数据记录与分析（图表）、用户学习进度跟踪、甚至简单的分子编辑器（JavaFX 2D绘图）。**这是对你化学背景和编程能力的最佳融合展示。**
2.  **分布式新闻/论文爬虫与分析系统**：爬虫调度器（Master）、多个爬虫Worker（可部署在不同机器）、统一的数据存储（数据库）、数据分析与可视化界面。综合运用多线程、网络、分布式、Web、数据库等技术。
3.  **为开源项目贡献代码**：选择一个你使用或感兴趣的开源Java项目（如Apache Commons、Guava、某些Minecraft Mod加载器），阅读其代码、文档、Issue列表，尝试修复一个bug或实现一个小的新功能，并向其提交Pull Request。**这是一个巨大的挑战，但能极大提升你的代码阅读和工程协作能力。**

